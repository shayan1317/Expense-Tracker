const Header = () => (
  <header className="w-full px-6 py-4 shadow-sm bg-white flex justify-between items-center">
    <h1 className="text-xl font-semibold">Expense Tracker</h1>
    {/* Add notifications or user menu if needed */}
  </header>
);

export default Header;
